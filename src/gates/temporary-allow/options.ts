import type { GateOptionsDefinition } from "../../core/options-contracts.js";

export const temporaryAllowGateOptions: GateOptionsDefinition = {
  cardDescription: "Fast domain access using the shared access defaults.",
  detailsSummary: "Details",
  notes: ["Uses the default temporary allow duration and access effects above."],
};
